[package]
name = "shing"
version = "0.1.0"
edition = "2021"
authors = ["ShingHQ <shinghq@users.noreply.github.com>"]
description = "A lightweight AI prompt template library"
license = "MIT OR Apache-2.0"
repository = "https://github.com/shinghq/shing"
keywords = ["ai", "prompt", "template", "llm"]
categories = ["template-engine", "text-processing"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
